# Environment for: netbooks/netZooPy/dragon_tutorial.ipynb
# Kernel name: pv5  |  display: PV5  |  language: python
# Build hint: clone pp5, then `mamba install` extras below.
# Post-install steps (run inside the env):
#   pip install netZooPy

name: pv5
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.10
  - ipykernel
  - jupyter
  - pip
