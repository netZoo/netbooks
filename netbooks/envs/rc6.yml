# Environment for: netbooks/netZooR/sex_differences_LUAD.ipynb
# Kernel name: rc6  |  display: RC6  |  language: R
# Build hint: clone rv9, then `mamba install` extras below.
# Post-install steps (run inside the env):
#   R -e 'remotes::install_github("netZoo/netZooR", upgrade="never")'

name: rc6
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - r-base=4.3.1
  - r-irkernel
  - jupyter
  - r-remotes
  - r-devtools
  - r-biocmanager
  - r-ggplot2
  - r-visnetwork
  - bioconductor-fgsea
