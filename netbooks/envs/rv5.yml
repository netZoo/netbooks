# Environment for: netbooks/netZooR/EGRET_toy_example.ipynb
# Kernel name: rv5  |  display: RV5  |  language: R
# Build hint: clone rv9, then `mamba install` extras below.
# Post-install steps (run inside the env):
#   R -e 'remotes::install_github("netZoo/netZooR", upgrade="never")'

name: rv5
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - r-base=4.3.1
  - r-irkernel
  - jupyter
  - r-remotes
  - r-devtools
  - r-biocmanager
