# /etc/systemd/journald.conf.d/netbooks.conf
# Cap journald disk use so we never silently fill the EC2 disk again.
[Journal]
SystemMaxUse=2G
SystemMaxFileSize=200M
MaxFileSec=1month
MaxRetentionSec=12week
